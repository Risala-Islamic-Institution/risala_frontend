version: "3"

services:
  risala_frontend:
    container_name: risala_frontend
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3333:3333
    environment:
      - NODE_ENV=production
